---
stage: "{{STAGE_NAME}}"
role: "{{STAGE_ROLE}}"
version: "1.0"
inputs:
{{FRONTMATTER_INPUTS}}
outputs:
{{FRONTMATTER_OUTPUTS}}
tools_allowed: {{FRONTMATTER_TOOLS}}
gate_criteria:
{{FRONTMATTER_GATE_CRITERIA}}
entry_criteria:
{{FRONTMATTER_ENTRY_CRITERIA}}
constraints:
{{FRONTMATTER_CONSTRAINTS}}
{{PARALLEL_STAGE_BLOCK}}{{FAN_OUT_BLOCK}}{{EVAL_STAGE_BLOCK}}---
<!-- acu-template: stage-claude.md — version 2026.04.17.3 -->
<!-- diataxis-primary: reference -->
# {{STAGE_NAME}} Stage

{{STAGE_DESCRIPTION}}

> **Mixed-mode doc** — primary quadrant: **Reference** (frontmatter, Objective, Approaches, Entry/Exit Gates). How-to sections: Methodology, Constraints, On Gate Failure. Each section below is tagged so you and the agent know which mode to apply.

<!-- quadrant: reference -->
## Objective

{{STAGE_OBJECTIVE}}

<!-- quadrant: how-to -->
## Methodology ({{METHODOLOGY_REF}})

{{METHODOLOGY_STEPS}}

<!-- quadrant: reference -->
## Approaches

| Approach | Purpose | Resources |
|----------|---------|-----------|
{{APPROACHES_TABLE}}

<!-- quadrant: reference -->
## Entry Gate (from {{PREV_STAGE}})

{{ENTRY_GATE_CRITERIA}}

<!-- quadrant: reference -->
## Exit Gate (to {{NEXT_STAGE}})

<!-- Convention: every criterion must be a testable statement.
     Format: "The {deliverable} contains {requirement}." or "The {artifact} passes {check}."
     Bad:  "Research is thorough."
     Good: "The research document contains at least 3 cited sources per claim." -->
{{EXIT_GATE_CRITERIA}}

<!-- quadrant: how-to -->
## Constraints

<!-- Convention: every constraint starts with a verb (Never, Always, Only, Do not).
     Bad:  "Security is important."
     Good: "Never store credentials in plaintext." -->
{{STAGE_CONSTRAINTS}}

<!-- quadrant: how-to -->
## On Gate Failure

If a gate transition from this stage fails, a `.gate-feedback.md` file will be written in the work unit directory containing the specific failure reasons.

To resolve:
1. Read `.gate-feedback.md` in the work unit directory for the specific failures
2. Address each `[FAIL]` item — these are the blocking issues
3. Review any `[WARN]` items — these are advisory but may indicate quality gaps
4. Re-run the gate after fixes: `bash gates/advance.sh <unit-dir> <transition>`

The feedback file is deleted automatically on the next successful gate pass.
